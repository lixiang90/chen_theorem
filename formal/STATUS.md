# 形式化审计与待完成证明

审计日期：2026-09-06。Lean / Mathlib：`v4.32.2`。

**项目尚未完成无条件形式化。** 主定理仍依赖一个 `sorry` 和一个额外公理。
这两项都是实质数学输入，不能用构建成功、隐藏警告、改名或将它们移为参数来宣称完成。

## 已核实的进度

原有 `ChenTheorem/` 数学部分有 96 个模块、约 74,000 行源码（不含本次迁入内容）。
以下区分证明本身与上游尚未证明的输入；源码行数不表示数学完成百分比。

| 部分 | 状态与依赖 |
| --- | --- |
| 引理 1、2、4、5 | 已有证明，包括平滑核、大筛、本原特征求和、筛分解 |
| 引理 3 | 已证明后续实际使用的、带高度对数项的四阶矩版本；论文印出的更强版本没有被冒充为已证定理 |
| 引理 6、方程 (12)–(21) | 代数、大导数范围和有限矩形轮廓步骤已有证明；小导数范围仍依赖零点自由区域输入 |
| 引理 7 | 已有证明；PNT 输入现在来自项目内的完整证明 |
| 引理 8 | Mertens、两次 Abel 求和与积分估计已有证明；通过引理 6 继承零点区域缺口 |
| 独立 Bombieri–Vinogradov 证明 | `statement_of_primitiveZeroFreeRegion` 是有明确假设的已证推论；无条件包装 `bombieriVinogradov` 仍含 `sorryAx` |
| 引理 9 | 原变量与固定平移两种情形的有限 Rosser 筛、BV 余项控制、普通计数转换、筛乘积归一化及幂次筛水平下的任意小误差吸收均已证明；Rosser 多项式的尖锐 `F/f` 界与主项最终组合仍未完成，原专门化仍是公理 |
| 积分 (24)、(27)、组合不等式 (28) | 已有证明 |
| 定理 1、定理 2及其定量形式 | 推导已有证明，但都继承下面两项未证明输入 |

## 本次完成的依赖迁移

- 删除 `lakefile.toml` 的 `PrimeNumberTheoremAnd` requirement。
- 锁文件只保留 mathlib 及其自身的 8 个传递依赖。删除 PNT 引入的
  `PrimeCert`、`leancert`、`checkdecls`、`LeanArchitect` 包依赖。
- 将所需的定量 PNT、Perron、Mertens 证明及其辅助内容迁至
  `ChenTheorem/Analysis/PNT/`：目前 19 个模块，14,655 行。
- 保留 Apache-2.0 许可证、来源提交、原始文件哈希和迁移说明。
  `scripts/vendor_pnt.py` 记录可重现的提取过程；重新生成时才需要原始固定提交的检出目录。
  正常构建完全不需要该检出目录。
- 删除蓝图注解与 Architect 导入，将证明内文档注释转换为普通注释。
- 不迁入 Wiener–Ikehara、数值表、ZetaSummary 等无关模块；Rosser–Schoenfeld
  文件只保留至 Mertens 第二定理。Perron 文件改为显式导入实际使用的 mathlib 积分引理。
- 新证明 `Chen.chebyshevPsi_isEquivalent` 从本地 `MediumPNT` 推出普通 PNT，
  替代原来的 `WeakPNT''` 导入。
- 为筛乘积归一化进一步复制了已有完整证明的 `IEANTN/Mertens.lean`，
  包含 Mertens 第三定理；没有恢复外部包依赖。提取脚本现跳过内容未变的文件。
- 移除 `Lemma6/Core.lean` 的 `set_option warn.sorry false`。

未修改主定理陈述，也没有增加任何数学假设来掩盖缺口。

## 本次补写的有限筛证明

`ChenTheorem/Lemma9/LinearSieve/Basic.lean` 为引理 9 补入四个有限求和步骤：

- `sum_divisors_multSum`：精确交换筛序列求和与公因子求和。
- `sum_le_siftedSum_of_lowerMoebius`：从下筛系数的逐点性质推得有限筛下界。
- `mainSum_sub_errSum_le_siftedSum`：将带符号余项替换为绝对值误差。
- `errSum_le_sum_of_level`：对绝对值不超过 1、支集截断在 `Q` 的系数，
  将筛误差控制为 `1 ≤ d ≤ Q` 的无权误差和。

这些定理补充了 mathlib 现有上筛接口所缺少的相应下界步骤。
它们没有断言 Richert 系数已经存在，也没有替换或消除现有公理。
四个定理均通过内核公理审计，仅依赖三个标准公理。

## 已补完：Rosser 有限筛系数构造

随后继续构造了实际筛系数，并修正了前述下筛性质的适用范围。
`IsLowerMoebius P mu` 现在只要求对 `n ∣ P` 成立。原先要求对所有自然数
成立的版本过强：如果下筛系数在 1 处为 1 且支集有限，取支集以外的素数
就不可能满足那个全局条件。有限筛实际只需要在筛素数乘积的因子上成立。

新增三个模块：

| 模块 | 已证明内容 |
| --- | --- |
| `LinearSieve/Rosser.lean` | 有限 Buchstab 恒等式、交替上下界递归、显式素因子串展开、展开一致性、严格递减与无重复、乘积低于 `D`、唯一分解与算术系数的绝对值及支集界 |
| `LinearSieve/RosserWeights.lean` | 算术系数和与素因子串的重编号、整除指标上的精确求值、局部下 Moebius 与全局上 Moebius 性质、`exists_rosser_sieve_weights` |
| `LinearSieve/RosserSieve.lean` | 任意乘法密度的主项等于递归多项式、构造出的上下筛权应用于 `BoundingSieve`、余项归约到 `1 ≤ d ≤ Q` 的无权误差和 |

这里采用三次截断的 Rosser 递归。下分支在剩余水平 `D ≤ z²` 时取零；
选择因子 `p` 后该条件变为 `p³ ≥ D`。系数由实际有限递归构造，
不存在“假设筛权存在”的新信任边界。精确主项仍保留为 `rosserEval`，
没有把所需的渐近估计作为已证结论。
数学构造可对照 [Tao 的筛法讲义，组合筛及 beta 筛部分](https://terrytao.wordpress.com/2015/01/21/254a-notes-4-some-sieve-theory/)。
这九个有限 Rosser 定理已通过内核公理审计，仅依赖
`propext`、`Classical.choice`、`Quot.sound`。

## 已补完：BV 与实际原变量素数筛的余项衔接

继续新增五个模块，所有结论均由已有定义和证明推导，没有添加新公理：

| 模块 | 已证明内容 |
| --- | --- |
| `BombieriVinogradov/PrimeWeights.lean` | 等差数列 θ 和、由 mathlib 的 `ψ−θ ≤ 2√x log x` 得到的素数幂损失、由 `Statement` 推出统一的 `ThetaStatement`、略去非既约剩余类的误差和 |
| `LinearSieve/PrimeSequence.lean` | 密度 `1/φ(d)` 的乘法性、以 `log p` 加权的实际序列 `x−p`、倍数和等于 `θ(x;d,x)` 的精确恒等式，以及 BV 控制其 Rosser 余项的推论 |
| `LinearSieve/PrimeSubsequence.lean` | 增加条件 `k ∣ x−p` 的实际子序列；主尺度为 `x/φ(k)`，余项恰为模 `d*k` 的 θ 误差；保留 `d` 整除小素数乘积的支集 |
| `LinearSieve/MiddlePrimeErrors.lean` | 小素数乘积因子 `d` 与较大素数 `k` 的分解 `d*k` 唯一；全部中间素数余项可由一次 BV 误差和控制，没有引入额外除数重数 |
| `LinearSieve/WeightedPrimeSieve.lean` | 主筛下界减去一半中间素数上界的有限组合；总损失至多为 `3/2` 倍的 θ 误差和，以及相应的统一 `O(x/log^A x)` 推论 |

`RosserSieve.lean` 也增加了保留实际除数支集的余项界。
这一限制对于中间素数求和很重要：若先把 `d` 扩大为全部 `d ≤ Q/k`，
就无法使用唯一分解得到上述无重数的 BV 控制。

这里的 BV 推论显式以已经定义的 `BombieriVinogradov.Statement` 为输入，
并不宣称该输入已无条件证明。它们是构造筛法与现有分析接口之间的新证明，
不是把主定理改成有条件结论。最终主定理及式 (26) 的陈述保持不变。
这一阶段得到对数加权计数的有限筛界；后续普通计数转换见下一节。
这些结果尚不能给出式 (26) 所需的尖锐渐近常数。

这一阶段的完整构建通过，22 个关键定理均通过内核公理审计，
只依赖 `propext`、`Classical.choice`、`Quot.sound`。

## 已补完：原变量普通素数计数转换

新增的五个模块将有限筛下界直接接到式 (26) 的原计数定义：

| 模块 | 已证明内容 |
| --- | --- |
| `LinearSieve/LogWeights.lean` | 有限集合的对数权上下计数转换；上界只对 `p < T` 的元素支付额外损失；主筛及中间素数子序列的具体转换 |
| `LinearSieve/SmallPrimeLoss.lean` | 非零 `m ≤ x` 至多有 10 个大于 `x^(1/10)` 的不同素因子；先交换两重计数，得全部中间素数的小素数损失至多 `10⌈T⌉`，乘一半后为 `5⌈T⌉` |
| `LinearSieve/ReducedPrimeCounts.lean` | 去掉整除 `x` 的筛素数，再恢复全剩余类，只损失至多 `3/2` 倍 `x.primeFactors.card`；非既约类只能包含整除模数的那个素数 |
| `LinearSieve/ChenPrimeCounts.lean` | `sievedPrimeCount`、`sievedPrimeCountAt` 与实际有限筛集合基数的精确等式；`chen_count_lower_of_rosser_polynomials` 给出直接针对式 (26) 原计数的构造性有限下界 |
| `LinearSieve/CountCutoff.lean` | 取 `T = x/(log x)^4`，对任意 `0 < ε < 1` 最终有 `T > 1` 且 `log T ≥ (1−ε) log x`，并控制取整损失 |

有限普通计数下界的主项分别除以 `log x` 和 `log T`，附加损失明确为
`5⌈T⌉ + (3/2) x.primeFactors.card`。主筛下界和中间素数总上界已提取为
`primeDifference_rosser_main_and_correction`，供这一步复用。
这里没有更改原 `sievedPrimeCount` 或式 (26) 的定义，也没有将未知的尖锐
Rosser 多项式渐近界当作已证明结论。

本阶段新增结论的内核审计已通过；该阶段共检查 36 个关键定理，
全部只依赖三个标准公理。

## 已补完：式 (25) 所需的筛乘积归一化

新增的本地 Mertens 第三定理及其自然数截断包装提供
`primeEulerProduct y * log y → exp(−γ)`。相应证明已通过内核审计，
没有从上游迁入未证明声明。

随后补入五个筛乘积模块：

| 模块 | 已证明内容 |
| --- | --- |
| `LinearSieve/SieveProduct.lean` | 实际密度乘积的精确分解：普通 Euler 素数乘积、有限孪生素数乘积、整除 `x` 的小素数修正因子 |
| `LinearSieve/DivisorProductTail.lean` | 修正因子与 `chenConst` 的精确关系；大于截断 `y` 的素因子尾部介于 `1−ω(x)/y` 与 `1` 之间 |
| `LinearSieve/SieveProductAsymptotics.lean` | 有限孪生素数乘积收敛至 `twinConst`；对满足 `ω(x)/y(x) → 0` 的截断统一归一化 |
| `LinearSieve/PowerSieveCutoff.lean` | 对任意固定 `a > 0`，截断 `⌊x^a⌋` 满足所需尾部条件，且 `log ⌊x^a⌋ / log x → a`；筛乘积归一化极限为 `2 exp(−γ)/a` |
| `LinearSieve/ChenSieveNormalization.lean` | 与实际 `chenReducedSmallPrimes` 集合的精确对应，以及 `chen_sieve_product_normalization`：实际筛乘积乘以 `log x / chenConst x` 趋于 `20 exp(−γ)` |

这给出式 (25) 在最终任意 `δ > 0` 扣损中需要的相对误差趋零形式。
没有把论文印出的更强 `O(1/log x)` 误差速率冒充为已证明结论。
本次归一化证明通过审计后，`AuditAnalysis.lean` 的 8 项与
该阶段 `AuditSieve.lean` 的 48 项全部仅依赖三个标准公理。

## 已补完：原变量计数损失的渐近吸收与具体筛水平

新增 `LinearSieve/CountLossAsymptotics.lean`、`CountAsymptoticSieve.lean`
与 `PowerSieveLevel.lean`，并补充 `CountCutoff.lean` 的对数比极限：

- `ceil(x/log⁴ x)·log² x/x → 0` 和 `ω(x)·log² x/x → 0`。
- `log T/log x → 1` 及其倒数极限，其中 `T = x/log⁴ x`。
- 使用 BV 指数三的余项估计后，两个不同对数分母下的余项、小素数
  截断损失与非既约类损失，统一不超过任意 `δ·xC_x/log² x`。
- 对任意固定 `1/3 < a < 1/2`，`Q = floor(x^a)` 最终满足 BV 水平，
  且对全部中间素数 `k ≤ x^(1/3)` 都有 `1 < floor(Q/k)`。
- `eventually_chen_count_lower_at_power_level` 将上述结果用于论文的真实
  普通计数，留下两个明确的递归 Rosser 主项。该定理显式以已定义的
  `BombieriVinogradov.Statement` 为假设；它不调用未证的 BV 包装，
  也不调用 Richert 公理。

这些结果补齐原变量误差与水平选择部分，尚未证明 Rosser 主项的尖锐
`F/f` 估计，因此不能据此删除或宣称已消除 Richert 公理。
该阶段 `AuditSieve.lean` 的 60 项检查全部通过，内核公理闭包均只包含
`propext`、`Classical.choice`、`Quot.sound`。

## 已补完：固定平移的完整有限筛与渐近误差框架

以下新增模块都位于 `ChenTheorem/Lemma9/LinearSieve/`：

| 模块 | 已证明内容 |
| --- | --- |
| `ShiftedPrimeSequence`、`ShiftedPrimeSubsequence` | 直接构造 `p+h`、`p ≤ x` 的对数加权序列；模 `d` 的 `-h` 剩余类及其互素性；精确主项 `x/φ(d)` 和固定中间素数后的模 `d*k` 余项 |
| `ShiftedWeightedPrimeSieve` | 主序列下界和中间素数总修正上界共用一个 BV 余项和；保留实际除数支集，避免引入额外重数 |
| `ShiftedLogWeights`、`ShiftedReducedPrimeCounts` | 当 `T+h ≤ x` 时，小素数总损失至多 `5 ceil(T)`；恢复非既约类损失至多 `3ω(h)/2` |
| `ShiftedChenPrimeCounts` | 与既有 `shiftedSievedPrimeCount`、`shiftedSievedPrimeCountAt` 定义的精确对应，以及两者的有限 Rosser 下界 |
| `ShiftedSieveNormalization` | 对每个固定非零 `h`，实际局部筛乘积乘以 `log x/C_h` 趋于 `20 exp(-γ)`；大素因子尾部最终恰为一 |
| `ShiftedCountLossAsymptotics` | `T=x/log⁴ x` 最终满足 `T+h ≤ x`；全部计数转换与 BV 余项可吸收到任意 `δ·xC_h/log² x` |
| `ShiftedCountAsymptoticSieve` | 在明确的 BV 假设下，以 `Q=floor(x^a)`、`1/3<a<1/2` 和全部子水平 `floor(Q/k)`，证明真实平移计数的渐近下界，保留两个待估计的 Rosser 主项 |

这些定理直接使用项目已有的平移计数（`p ≤ x`），没有改动计数定义或
截短为另一问题。它们没有调用 `richert_bombieri_equation26`，也没有
调用仍含 `sorryAx` 的无条件 BV 包装。要消除原公理，仍必须证明
两种情形共同需要的尖锐线性筛主项界，并完成最终主项组合。
上述结果加入后，`AuditSieve.lean` 的 84 项检查全部通过，均只依赖
`propext`、`Classical.choice`、`Quot.sound`。

## 已补完：定量一维筛密度条件与 Rosser 误差递推

新增以下 `LinearSieve/` 模块，继续推进尖锐线性筛证明的分析前提：

| 模块 | 已证明内容 |
| --- | --- |
| `DensityInterval` | 两个截断点独立趋于无穷时的归一化乘积比极限；对任意奇素数子集，区间逆乘积的一致 `1+ε` 界 |
| `DensityRatio` | 任意截断下 `sieveProduct` 与实际筛集合的精确对应、正性和区间乘积比恒等式，可用于递推内部的截断点 |
| `DensityQuantitative` | 复用本地已证的 Mertens 第三定理误差界，并证明整数修正因子的精确望远镜乘积；得到同一个 `K>0` 对所有自然数端点 `2≤w≤z`、所有奇素数子集均成立的 `(1+K/log w)·log z/log w` 界 |
| `RosserDefect` | 上筛减局部乘积、局部乘积减下筛的非负性；停止条件与非负求和递推；归一化误差递推以及从误差恢复上下筛主项的精确公式 |
| `BuchstabMeasure` | 单个素数的乘积跳跃、区间质量求和、归一化区间质量的精确 `V(w+1)/V(z+1)-1` 公式和定量一维界 |

定量密度定理显式量化整个有限奇素数集合，因此同时覆盖依赖 `x` 的
原变量筛与固定平移筛，不需要另加密度公理。上述定理使用自然数端点
和 `(w,z]` 区间；下节补入实端点版本和加权积分比较。

分析路线参考 Yuta Suzuki 的
[*The Rosser–Iwaniec Sieve*](https://www.suzuyu1729.com/notes/NagoyaSeminar2022/RosserIwaniec.pdf)，
尤其第 7–14 节。参考文献用于定位证明步骤，所有已声明结果均有本地
Lean 证明。下一步仍需与连续延迟方程的比较、迭代余项
控制及 `2e^γ` 常数校准；目前不能据此宣称尖锐 `F/f` 主项已证。
新增定量密度与递推结果加入审计后，`AuditSieve.lean` 共 104 项检查
全部通过，内核公理闭包仅含三个标准公理。

## 已补完：实端点密度界与带权 Buchstab 分部求和

新增五个 `LinearSieve/` 模块：

| 模块 | 已证明内容 |
| --- | --- |
| `AbelTail` | 有限系数尾和形式的 Abel 恒等式、尾和乘导数的可积性，以及非负递增权重的上界传递 |
| `DensityReal` | `log floor(t)` 的一致误差界；将自然数端点密度界扩展到所有实数 `2≤w≤z`，保留 `1+K/log w` 的误差形式 |
| `WeightedBuchstab` | 实际归一化 Buchstab 原子、前缀和与实数截断尾和的精确公式；加权素数和等于边界项加尾和积分；应用已证密度界 |
| `BuchstabIntegral` | 对连续尾和上界分部积分，得到密度 `log z/(t log²t)·(1+2K/log t)` 和终点边界项 |
| `BuchstabError` | 对数积分核的精确积分，以及将连续密度修正吸收为显式 `2K f(z)/log w` 误差 |

最终的 `weighted_buchstab_partial_summation` 对同一个 `K>0`、任意有限奇素数
集合 `P`、实数 `w≥2` 和整数 `z≥w` 成立。记 `V(u)` 为该集合的局部筛乘积，
对非负可微权重 `f`，假设导数可积且非负、
`f(t) log z ≤ f(z) log t`（`w≤t≤z`），证明了

```text
Σ_{w<p≤z, p∈P} f(p) ν(p) V(p)/V(z+1)
  ≤ log z · ∫_{(w,z]} f(t)/(t log²t) dt + 2K f(z)/log w.
```

所有取整、正性、可积性及积分边界项均在 Lean 中处理，没有增加密度或
分部求和公理。当前版本对闭区间上处处可微的权重给出结论；仍需按实际
延迟方程函数的分段光滑性应用或扩展；下面继续补入变量替换和单步连续比较。
这些结果并未消除下面两个主定理缺口。

本节新增 18 项内核检查后，`AuditSieve.lean` 共 122 项全部通过，
公理闭包均只含 `propext`、`Classical.choice`、`Quot.sound`。

## 已补完：筛参数换元与 Rosser 单步积分比较

新增四个 `LinearSieve/` 模块：

| 模块 | 已证明内容 |
| --- | --- |
| `SieveParameter` | `sieveParameter D t = log D/log t` 的正性、反单调性、导数和区间映射；精确换元公式；选择素数后 `log(D/p)/log p = log D/log p - 1` |
| `BuchstabParameter` | 对复合权重 `H(log D/log t)` 验证导数、可积性和对数增长条件；得到主项 `(1/s) ∫_s^σ H` 与误差 `2K H(s)/log w` |
| `RosserIntegralStep` | 将实际归一化 Rosser 误差递推精确拆成小素数前缀和大素数区间；在明确的后继误差上界假设下，将大素数部分控制为参数积分 |
| `RosserExactLevel` | 构造只依赖截断的有限水平阈值；超过阈值后上下 Rosser 主项精确等于局部乘积；固定小素数截断的前缀误差最终精确为零，对 `P`、终点和上下筛选择一致 |

参数积分定理假设 `H` 在相关闭区间可微、导数连续且非正、`H≥0`，
以及 `s H(s)` 反单调。它验证这些条件如何传到已有的加权分部求和定理，
没有将它们断言为实际延迟方程函数已满足的性质。

记前缀误差为 `R_small`，当 Rosser 递推未触发下筛停止条件，且后继误差
在大素数区间逐点不超过 `H(log D/log p)` 时，已证单步结论为

```text
δ_upper_or_lower(P, z+1, D)
  ≤ R_small + (1/s) ∫_{(s,σ]} H(v) dv + 2K H(s)/log w,
s = log D/log z,  σ = log D/log w.
```

固定 `m=floor w` 时，`D > rosserExactLevel(m+1)` 保证 `R_small=0`。
这是固定截断的精确结论，尚未提供随 `D` 增长的截断所需的最优阈值估计。
剩余分析工作包括连续项级数的收敛、验证停止区间和完整递推比较、
控制迭代余项，并校准 `2e^γ`。下一节补入连续项的实际构造。

本节新增 22 项内核检查后，`AuditSieve.lean` 共 144 项全部通过，
均只依赖三个标准公理。

## 已补完：连续项构造与有限和的延迟微分关系

新增五个 `LinearSieve/` 模块：

| 模块 | 已证明内容 |
| --- | --- |
| `ContinuousTail` | 将积分下端截断到有限区间的尾积分算子；非负性、连续性、紧支集和参数加权单调性 |
| `ContinuousTerms` | `β=2` 的实际连续递推项 `f_n`；`f₁(s)=max(3-s,0)/s`、非负性、连续性、`f_n` 与 `s f_n` 反单调、`s≥n+2` 时为零 |
| `ContinuousTailDerivative` | 在积分函数于上端之后为零的条件下消去上端截断；严格证明尾积分及其参数乘积的导数，包括上支集边界 |
| `ContinuousTermDerivative` | `n≥2` 的 `(s f_n(s))′=-f_(n-1)(s-1)`、导数连续且非正，适用范围为对应下截断点以上；下截断点以下 `s f_n(s)` 恒定；移位项满足所需加权反单调性 |
| `ContinuousPartials` | 实际奇、偶项有限和的连续性、非负性、随项数单调增长、参数加权反单调，以及有限和之间的延迟微分关系 |

具体地，令 `c_n=2`（`n` 偶）或 `3`（`n` 奇）。对 `n≥2`，定义

```text
f_n(s) = (1/s) ∫_{(max(s,c_n), n+2]} f_(n-1)(t-1) dt   (s>0),
```

其中空区间积分为零；源码以截断下端的等价形式确保积分不会反向。
函数在所有正参数上定义，实际筛比较仍使用相应奇偶项的有效区间。
现已证明的有限和关系为

```text
U_N(s) = Σ_{k=0}^N f_(2k+1)(s),   L_N(s) = Σ_{k=0}^{N-1} f_(2k+2)(s),
(s U_N(s))′ = -L_N(s-1)        (s>3),
(s L_(N+1)(s))′ = -U_N(s-1)    (s>2).
```

上述有限和结果没有假定非负级数收敛。下节补入有限和的一致上界、级数收敛
和无穷和求导；实际离散筛的迭代误差及极限常数校准仍待完成。

本节新增 39 项内核检查后，`AuditSieve.lean` 共 183 项全部通过，
公理闭包均仅含三个标准公理。

## 已补完：连续级数收敛与极限延迟方程

新增五个 `LinearSieve/` 模块：

| 模块 | 已证明内容 |
| --- | --- |
| `ContinuousTailMass` | 有限连续尾积分的三角积分恒等式，以及参数乘积的积分质量公式 |
| `ContinuousMass` | 奇偶连续项的非负加权质量；奇项到偶项的精确质量损失、偶项到奇项的质量保持，以及初始质量等于 2 |
| `ContinuousConvergence` | 望远镜质量恒等式，偶项有限和统一不超过 1；奇项的积分控制；两类级数在实际有效区间的收敛及和的上界 |
| `ContinuousErrors` | 定义收敛的上下误差函数；在远离开端点的整个半直线上一致收敛，极限连续且参数加权反单调 |
| `ContinuousErrorDerivative` | 以已收敛的另一类级数作为导数控制，严格交换无穷求和与求导，证明极限误差函数的延迟微分关系 |

收敛证明使用本地证明的非负质量恒等式。记

```text
O_N = ∫_1^(2N+3) s f_(2N+1)(s) ds,
E_N = ∫_2^(2N+4) s f_(2N+2)(s) ds.
```

已证 `O_0=2`、`O_(N+1)=E_N`、`E_N+2 f_(2N+2)(2)=O_N`，因此
`O_N+2 L_N(2)=2`。利用 `O_N≥0`，得到所有 `N` 的 `L_N(2)≤1`。
另由下一偶项的定义积分与单调性，控制每个奇项在任意固定 `s>1` 的值。
这给出实际级数的收敛，并未将收敛作为假设加入接口。

令 `T⁺`、`T⁻` 分别为奇、偶项级数之和。现在已证：

```text
0 ≤ T⁻(s) ≤ 1                         (s≥2),
0 ≤ T⁺(s) ≤ 2/(min(s,2)-1)             (s>1),
(s T⁻(s))′ = -T⁺(s-1)                 (s>2),
(s T⁺(s))′ = -T⁻(s-1)                 (s>3).
```

这些极限性质仍不等于尖锐筛定理。下节进一步证明无穷远行为和初值；
`2e^γ` 常数校准、分段边界处理、与离散 Rosser 筛的统一误差比较和最后的主项组合仍待完成。
零点自由区域的 `sorry` 与引理 9 的公理仍然存在。

本节新增 31 项内核检查后，`AuditSieve.lean` 共 214 项全部通过，
均只依赖 `propext`、`Classical.choice`、`Quot.sound`。

## 已补完：连续筛的衰减、端点值与初始区间公式

新增 `ContinuousErrorDecay`、`ContinuousInitial`、`ContinuousPairing`、
`ContinuousEndpoint`、`ContinuousFunctions` 五个模块。

首先利用每个递推项的紧支集及可求和的一致控制，证明
`s T⁺(s) → 0`、`s T⁻(s) → 0`，进而证明两个误差本身趋于零。
这里使用有支配条件的级数极限交换，没有把衰减作为新假设。

令 `G(s) = s(T⁺(s)+T⁻(s))`。从已证延迟方程经有限区间积分，得到

```text
(s-1) G(s) - ∫_(s-1)^s G(t) dt = 2 G(3) - ∫_2^3 G(t) dt    (s≥3).
```

`G` 非负、单调递减且趋于零，故右端常数非负。使用初始区间积分公式，
右端精确等于 `2(T⁻(2)-1)`。结合此前的上界，得到 `T⁻(2)=1`，
并进一步证明上述守恒量恒为零。

现在从实际收敛级数定义 `F=1+T⁺`、`f=1-T⁻`，并证明

```text
F(s) = A/s                          (1<s≤3),
f(s) = A log(s-1)/s                  (2≤s≤4),
f(2) = 0,
(s F(s))′ = f(s-1)                  (s>3),
(s f(s))′ = F(s-1)                  (s>2),
F(s) → 1,  f(s) → 1                (s→∞),
A := 3(1+T⁺(3)),  A≥3.
```

这些函数及其公式均由证明构造。**仍未证明 `A=2e^γ`**，也没有把
`F/f` 对离散筛的控制作为已完成结论；这两部分必须继续证明。

本节新增 36 项内核检查全部通过，`AuditSieve.lean` 现共 250 项，
均只依赖三个标准公理，没有继承现存的两项未证明输入。

## 已补完：实际连续误差函数的 Rosser 单步比较

新增 `ContinuousErrorRegularity`、`ContinuousUpperSmooth`、
`ContinuousErrorIntegrals`、`ContinuousBuchstab`、`RosserContinuousStep`
五个模块，将前面的抽象光滑权重接口应用于已经构造的实际误差级数。

- 证明两个误差函数本身的单调性、移位后的加权单调性及连续导数。
- 处理上筛误差的 `s=3` 拼接点：初始公式给出左导数，延迟积分给出右导数，
  `T⁻(2)=1` 保证二者相等。因此 `T⁺` 在整个 `s>1` 上连续可微。
- 从延迟方程证明精确积分恒等式，保留另一端的非负尾项，再导出积分上界。
- 得到实际移位误差函数的素数加权 Buchstab 界，并代入有限 Rosser 递推。

写 `s=log D/log z`，`s_p=log(D/p)/log p`。在原来的奇素数集合、
`D>1`、`2≤w≤z` 条件下，有统一 `K>0`，对任意 `M≥0`：

```text
若 δ⁻(P,p,D/p) ≤ M T⁻(s_p) 对 w<p≤z 的筛素数成立，则
δ⁺(P,z+1,D) ≤ prefix⁺ + M T⁺(s) + 2KM T⁻(s-1)/log w       (s>3)。

若 δ⁺(P,p,D/p) ≤ M T⁺(s_p) 对 w<p≤z 的筛素数成立，则
δ⁻(P,z+1,D) ≤ prefix⁻ + M T⁻(s) + 2KM T⁺(s-1)/log w       (s>2)，
其中下支还要求实际递推处于活动状态 D>(z+1)²。
```

若 `D>rosserExactLevel(floor(w)+1)`，两个前缀均精确消失。
新接口不再假设任意权重的光滑性、形状或积分界；这些条件均已为实际函数证明。
**对子状态的估计仍是显式归纳条件。** 完整迭代、误差的统一吸收、
上支 `s≤3` 的分段比较，以及归一化常数 `A=2e^γ` 仍未证明，
不能以这个单步定理替代最终的尖锐筛定理。

本节新增 28 项内核公理检查全部通过，`AuditSieve.lean` 现共 278 项，
仍仅依赖三个标准公理。

## 已补完：上筛小参数区间的精确拼接与统一归约

新增 `RosserUpperTransport`、`RosserUpperComparison`、`RosserCubeCutoff`、
`RosserUpperInitial` 四个模块。

首先证明有限 Buchstab 系数及前缀在不同终点之间的精确缩放。
若 `m≤z` 且 `D≤(m+1)³`，所有后续下筛分支均停止，因而

```text
R⁺(P,z+1,D) = R⁺(P,m+1,D),
1+δ⁺(P,z+1,D) = [V(P,m+1)/V(P,z+1)] · [1+δ⁺(P,m+1,D)].
```

将已证定量乘积比代入，得到：若
`s_m(1+δ⁺(P,m+1,D))≤A+e`，则在 `1<s≤3` 有

```text
δ⁺(P,z+1,D) ≤ T⁺(s) + e/s + K(A+e)/(log(m)·s),
s_m=log D/log m,  s=log D/log z.
```

然后定义严格立方根截断 `m(D)=ceil(D^(1/3))-1`，证明
`m(D)³<D≤(m(D)+1)³`、`m(D)→∞`、`s_m>3`（充分大时）且 `s_m→3`。
该取整约定也覆盖 `D` 恰为立方的情形。连续筛的加权主项
`s_m F(s_m)` 因而趋于 `A`。

最终得到统一的条件化归约：对每个 `ε>0`，充分大的 `D` 下，
若 `δ⁺(P,m(D)+1,D)≤T⁺(s_m)+ε/16`，则对所有有限奇素数集合 `P`
及所有满足 `z≥2`、`1<s≤3` 的终点，均有
`δ⁺(P,z+1,D)≤T⁺(s)+ε`。其中立方条件和 `m(D)≤z` 都已证明，
不再作为附加假设留给调用方。

这完成了小参数区间到 `s>3` 一侧的归约。**截断处的局部误差估计仍未证明**；
它与此前的子状态条件一起，仍需通过完整 Rosser 迭代消除。
本节不消除引理 9 的最终公理，也不包含 `A=2e^γ` 的常数校准。

本节新增 22 项内核公理检查全部通过，`AuditSieve.lean` 现共 300 项，
均仅依赖 `propext`、`Classical.choice`、`Quot.sound`。

## 已补完：停止深度展开、阶乘尾项与大参数界

新增 `RosserStoppingMass`、`RosserChainBound`、`RosserDepthTail`、
`RosserMassLogBound`、`RosserLargeParameter` 五个模块。

定义 `r_n(P,z,upper,D)` 为实际 Rosser 递推在恰好选取 `n` 个递减素数后
首次停止的贡献，证明其非负、深度超过 `z` 时为零，且

```text
rosserDefect(P,z,upper,D) = Σ_(0≤n≤z) r_n(P,z,upper,D).
```

这与已有 Rosser 多项式精确相等，并非另一个假设性的模型。
若 `D>z^(n+2)`，对应深度项也精确为零。

对所有递减素数链的无约束质量，利用递推及 Bernoulli 不等式证明阶乘界。
写 `L=Σ_(p<z,p∈P) 1/φ(p)`，则 `r_n≤L^n/n!`，从而对任意深度 `N` 有

```text
0 ≤ rosserDefect(P,z,upper,D) - Σ_(n<N) r_n(P,z,upper,D)
  ≤ exp(L) · L^N/N!.
```

已证一维密度条件进一步给出统一 `K>0`。设
`B_z=(1+K/log 2)·log z/log 2`，则对 `z≥2`，终点为 `z+1` 时的
密度和满足 `L≤log B_z`，且 `exp(L)≤B_z`、`1/V(P,z+1)≤B_z`。
若 `D>(z+1)^(N+1)`，全部浅层贡献为零，得到无条件的粗界

```text
rosserRelativeDefect(P,z+1,upper,D) ≤ B_z² · (log B_z)^N/N!.
```

以上估计对原先的有限奇素数集合统一成立，没有子状态或连续主项比较假设。
它们提供了深度截断和大参数控制；**仍需证明离散深度项与连续奇偶项的
统一比较，并完成迭代误差及参数选择**，才能消除引理 9 公理。
常数校准和零点自由区域也仍未完成。

本节新增 25 项内核公理检查全部通过，`AuditSieve.lean` 现共 325 项，
均仅依赖三个标准公理。

## 已补完：深度奇偶结构与首层离散—连续比较

新增 `RosserStoppingParity`、`RosserFirstMass`、`RosserFirstComparison`、
`RosserDepthRecursion` 四个模块。

上筛的偶数深度项、下筛的奇数深度项精确为零。定义每个深度项除以实际
筛乘积的归一化版本，证明归一化误差仍等于这些项的有限和。

首层项现在有精确公式。写 `m=ceil(D^(1/3))-1`，若 `m≤z`，则

```text
r_1(P,z+1,true,D) = V(P,m+1) - V(P,z+1)；
```

若 `z≤m`，首层项为零。该公式保留了精确立方与整数端点的停止约定。
令 `s=log D/log z`、`σ=log D/log m`，由定量密度条件得到

```text
r_1(P,z+1,true,D)/V(P,z+1)
  ≤ f_1(s) + (σ-3)/s + Kσ/(log(m)·s),
f_1(s)=max(3-s,0)/s。
```

这里 `D>1`、`m≥2`、`z≥2`、`s>1`。两项显式误差趋于零，因此首层比较
对所有有限奇素数集合与所有 `s>1` 一致成立：给定任意 `ε>0`，充分大
的 `D` 下，归一化首层项不超过 `f_1(s)+ε`。没有子递推估计假设。

对一般深度，还证明了归一化递推及小素数前缀的精确拆分。父项深度为
`n+1` 时，只需 `D>m^(n+3)`，其 `p≤m` 前缀就精确消失。
这为固定深度使用随 `D` 增长的分割点提供了显式幂次条件。

**更高深度的完整离散比较、随深度变化的统一误差控制与最终累积仍未完成。**
首层比较及有限递推接口还不能消除引理 9 的公理，常数校准与零点自由区域仍待证明。

本节新增 18 项内核公理检查全部通过，完成本节时 `AuditSieve.lean` 共 343 项，
均仅依赖三个标准公理。

## 已完成：递归深度项在拼接点处的 Buchstab 积分比较

新增 `AbelRight.lean`、`BuchstabRight.lean`、`ContinuousTermJunction.lean`
和 `BuchstabTerm.lean`。Abel 求和与 Buchstab 估计现允许连续函数具有可积右导数，
不再要求其在每个拼接点都具有双侧导数。Abel 证明由 mathlib 的原证明改写，
保留原作者和 Apache-2.0 许可说明。

对每个递归项 `f_(n+2)`，已证明全部正参数处的左导数。低于其下截断点时，
`s f_(n+2)(s)` 是常数；高于截断点时，其导数是 `-f_(n+1)(s-1)`。
对应的跳跃源项可积。由于 `log D/log t` 严格递减，这个左导数经复合成为
素数大小变量中的右导数，恰好适用新的 Abel 公式。

因此，令 `s=log D/log z`，在 `s≥cutoff(n+3)` 时已证明

```text
Σ_(w<p≤z) f_(n+2)(log D/log p - 1) · ν(p)V(p)/V(z+1)
  ≤ f_(n+3)(s) + 2K f_(n+2)(s-1)/log w.
```

该结论对所有 `n≥0` 使用同一个密度常数 `K`，允许子项的下截断点落在积分区间内部，
也允许父参数等于其下截断点。证明先给出精确连续积分及非负终端尾项，再舍去尾项。
这里没有离散子状态估计假设。

这一步还没有完成全部停止深度的比较；首项拼接与离散误差的单步传播见下一节，
随深度增长的统一控制与最终累积仍需处理。新增 18 项内核检查通过，
完成本节时 `AuditSieve.lean` 共 361 项，全部仅依赖三个标准公理。

## 已完成：首项拼接、离散误差传播与第二层显式比较

新增 `BuchstabFirstTerm.lean`、`RosserDepthComparison.lean`、
`RosserSecondComparison.lean`。首项 `f_1(s)=max(3-s,0)/s` 在 `s=3`
的左导数已证明，并用于 Buchstab 比较。现在存在同一个密度常数 `K>0`，
使连续比较覆盖所有正深度，包括 `f_1` 到 `f_2` 的步骤。

离散深度递推中，若每个子项的加性误差不超过 `E`，其累计误差精确由

```text
E · (V(m+1)/V(z+1) - 1)
```

控制。这里保留真实的 Buchstab 尾质量，不以素数个数替代。父深度为 `n+2` 时，
`D>floor(w)^(n+4)` 消去小素数前缀；随后主项由 `f_(n+2)(s)` 控制，
并保留密度误差 `2K f_(n+1)(s-1)/log w`。一般深度的这一步仍有子项比较假设。

对于第二层，该假设已通过已证明的首层比较消除：对任意 `ε>0`，存在 `T>1`，
当 `D/z≥T`、`D>floor(w)^4`、`s=log D/log z>2` 且下筛处于活动状态时，

```text
relativeStoppingMass(P,2,z+1,false,D)
  ≤ f_2(s) + 2K f_1(s-1)/log w
      + ε · (V(floor(w)+1)/V(z+1) - 1).
```

结论对所有有限奇素数集合统一成立，不再含子状态假设。证明也处理了子截断点
`p` 与首层定理中 `z+1` 的差异，利用连续首项的单调性完成转换。
第二层的统一小误差形式现已在下一节完成；
任意深度的迭代及其累计误差仍待证明。这些进展尚不能消除引理 9 的公理。
本节新增 10 项内核审计全部通过，筛法审计累计 371 项。

## 已完成：第二层停止项的统一连续比较

新增 `RosserSecondUniform.lean`。取 `w=D^(1/8)`，已在 Lean 中证明：

- `w^8=D`、`log w=log D/8`，且 `w` 趋于无穷；
- 大 `D` 下，`floor(w)^4<D`，因此第二层小素数前缀为零；
- 若终端 `z<w`，则 `(z+1)^4<D`，第二层停止项整体为零；
- 下筛活动时 `s=log D/log z>2`，从而 `log z/log w≤4`；
- 利用已证明的密度条件，归一化 Buchstab 尾质量最终统一不超过 `8`。

将这些估计代入上一节的无子状态假设界，并把首层误差选为 `ε/16`，
所得最终结论为

```text
∀ ε>0, eventually D, ∀ z≥2, ∀ finite sets P of odd primes,
  relativeStoppingMass(P,2,z+1,false,D) ≤ f_2(log D/log z) + ε.
```

停止情形与小终端情形均在证明内处理，结论不再要求指定分割点、最小子水平、
活动状态或参数区间。供下一层递推使用的截断版本
`relativeStoppingMass(P,2,p,false,D) ≤ f_2(log D/log p)+ε` 也已证明，
对所有 `p≥3` 统一成立。

该结论完成了实际第二层离散停止项的统一比较，但尚未推广至所有深度。
高阶上筛在小参数区间的转移现已在下一节完成；
统一迭代误差、最终累积及 `2e^γ` 校准仍未完成。
本节新增 10 项内核审计全部通过，筛法审计累计 381 项。

## 已完成：任意固定上筛深度的小参数转移

新增 `RosserDepthTransport.lean` 和 `RosserDepthInitial.lean`。对于每个深度
`j≥2`，下筛立即停止时该正深度项为零。因此，当 `m≤z` 且 `D≤(m+1)^3` 时，
已证明精确恒等式

```text
relativeStoppingMass(P,j,z+1,true,D)
  = V(m+1)/V(z+1) · relativeStoppingMass(P,j,m+1,true,D).
```

这与完整上筛缺陷的转移有所不同：此处没有加一减一项，后者属于首次停止的深度贡献。
由密度条件，截断点的加权分子上界 `σ r_j(m+1)≤B` 可转移为
`r_j(z+1)≤B/s+KB/(log m·s)`，其中密度常数对所有深度相同。

对下截断点为 `3` 的连续项，`s f_j(s)=3 f_j(3)` 在 `0<s≤3` 成立，
而 `σ f_j(σ)≤3 f_j(3)` 在 `σ>3` 成立。取严格整数立方根截断点后，
`3<σ<4` 最终成立。因此，该点误差 `ε/16` 足以推出整个 `1<s≤3`
区间的误差 `ε`，密度误差通过 `log m→∞` 吸收。

最终接口 `rosser_depth_upper_comparison_of_large_parameter` 已证明：
任意固定深度 `j≥2` 且连续项下截断点为 `3` 时，若参数 `s>3` 的统一比较
对每个正误差成立，则整个 `s>1` 区间的统一比较成立。
此处的大参数比较假设现已通过下一节的一般固定深度归纳消除，未作为额外公理加入。
本节新增 9 项内核审计全部通过，筛法审计累计 390 项。

## 已完成：所有固定正深度的统一离散—连续比较

新增 `RosserDepthSplit.lean`、`RosserDepthStep.lean` 与 `RosserFixedDepth.lean`。
第二层的分割方法现推广为 `w=D^(1/(2k))`。对从深度 `n+1` 到 `n+2` 的步骤，
取 `k=n+4`，得到前缀消失、短终端区间整体为零、`log z/log w≤k`
及最终尾质量不超过 `2k`。子项误差选为 `ε/(4k)`；密度误差由
`f_(n+1)(s-1)≤f_(n+1)(1)` 与 `log w→∞` 控制。

通用单步引理已证明：子深度的统一比较推出父深度在其连续下截断点以上的统一比较，
不再要求外部给定分割点、最小子水平或离散子项界。随后，以第一层为归纳起点：

- 与筛类型不匹配的深度由奇偶结构精确为零；
- 下筛的活动分支自动满足 `s>2`，停止分支为零；
- 上筛的 `s>3` 结果通过上一节的精确转移扩展到全部 `s>1`。

最终 `rosserDepthBound_all` 与其展开版本证明

```text
∀ n≥1, ∀ upper, ∀ ε>0, eventually D,
  ∀ z≥2 with log D/log z>1, ∀ finite sets P of odd primes,
    relativeStoppingMass(P,n,z+1,upper,D) ≤ f_n(log D/log z)+ε.
```

固定有限个正深度的和也已证明有同样的任意小总误差。
**阈值允许依赖深度或求和项数。** 当前结果不能直接用于随 `D` 增长的深度截断，
也不能交换两个极限。下一节控制了增长深度以后的尾项，但前段增长项数的误差
仍需定量控制，才能完成总缺陷的尖锐比较；
`2e^γ` 校准和零点自由区域也仍未完成。
本节新增 14 项内核审计全部通过，筛法审计累计 404 项。

## 已补完：增长深度后的统一尾界

`RosserGrowingTail.lean` 将实际相对缺陷的深度余项控制为
`B(z)^2 (log B(z))^N / N!`，其中
`B(z) = (1 + K/log 2) log z/log 2`，`K>0` 对所有有限奇素数集合统一。
利用指数级数先证明该界不超过 `B(z)^4/2^N`，再取

```text
N(z) = ceil(6 log B(z) / log 2).
```

于是截断余项非负且不超过 `1/B(z)^2`，随 `z→∞` 趋于零。
这对任意总水平 `D`、上下筛分支和有限奇素数集合统一成立。
`N(z)` 的取整上下界也已证明；其增长尺度是 `log log z`。
新增 10 项内核审计通过，筛法审计累计 414 项。

这没有消除前段的证明义务：固定项数比较的阈值可能依赖项数，不能直接用于
上述 `N(z)`。仍需有定量误差的深度比较来控制前段增长项数的和。

## 已补完：定量递推误差的辅助连续函数

`ContinuousAuxiliary.lean` 从已经构造的误差 `U,L` 定义辅助误差 `Û,Ĺ`。
在分析区间内它们分别等于 `-U'`、`-L'`，并且满足

```text
(s² Û(s))' = -s Ĺ(s-1)       (s>3),
(s² Ĺ(s))' = -s Û(s-1)       (s>2).
```

设 `A=linearSieveInitialConstant`，初始公式为 `Û(s)=A/s²`（`1<s≤3`）
及 `Ĺ(s)=2A/s²`（`0<s≤2`）。已证明非负性、相应定义域的连续性，
以及 `s²Û`、`s²Ĺ` 的单调递减。下支在 `s=2` 的连续拼接使用
`A*(s/(s-1)-log(s-1))/s²` 的右侧初始公式。
这里没有假定尚未证明的常数校准 `A=2e^γ`。

`ContinuousAuxiliaryIntegrals.lean` 进一步证明 `s²Û(s)` 与 `s²Ĺ(s)`
在无穷远趋于零，以及保留尾项的精确积分恒等式，例如

```text
∫[s,b] t Ĺ(t-1) dt = s²Û(s) - b²Û(b)       (3≤s≤b).
```

交换上下支的公式对 `2≤s≤b` 成立；在 `s=2` 使用连续延拓及几乎处处
相等处理上支移位端点。两式均已推出上端趋于无穷远的积分极限和有限区间上界。
这些是后续定量误差递推的输入；尚未证明相应的离散误差控制或所需的更强衰减率，
不能据此声称前段增长项数的误差已经收敛。

这些函数满足 Suzuki 的 [Rosser–Iwaniec 筛讲义第 13 节](https://www.suzuyu1729.com/notes/NagoyaSeminar2022/RosserIwaniec.pdf)
在维数一时的辅助延迟方程和初始比例。下一节已证明带因子
`((t-1)/t)^θ` 的有限区间非严格积分控制；第 14 节所需的完整定量递推误差
仍未完成。当前只完成明确列出的 Lean 定理，尚未形式化该讲义的完整定量引理。
本节新增 27 项内核审计通过，筛法审计累计 441 项。

## 已补完：带权辅助积分与子水平对数误差

`ContinuousAuxiliaryWeights.lean` 定义
`a_δ(t) = (t/(t-1))^δ (t-1)`，证明它等于
`((t-1)/t)^(1-δ) t`。当 `t>1, δ≤1` 时，`0≤a_δ(t)≤t`。
两种辅助积分均得到保留尾项的上界，例如

```text
∫[s,b] a_δ(t) Ĺ(t-1) dt ≤ s²Û(s) - b²Û(b)    (3≤s≤b),
(1/s) ∫[s,b] a_δ(t) Ĺ(t-1) dt ≤ sÛ(s).
```

交换上下支的带权公式目前要求 `2<s≤b`。这是有限区间的非严格上界，
不冒充讲义中的严格不等式或包含端点的全部带权结论。
当 `δ≥-1` 时，借助精确恒等式
`t a_δ(t) H(t-1) = (t/(t-1))^(δ+1) (t-1)² H(t-1)`，
还证明了两种移位辅助函数乘上 `t a_δ(t)` 后的单调递减性。

`AuxiliaryLevelError.lean` 定义基本误差尺度
`E_δ(D,s;H)=(log D)^(-δ) s H(s)`。令 `t=log D/log p>1`，已证明

```text
log(D/p) = log D * (t-1)/t,
E_δ(D/p,t-1;H) = (log D)^(-δ) a_δ(t) H(t-1).
```

因此上述积分不等式确实控制子水平 `D/p` 产生的误差尺度，
并分别被父水平的 `E_δ(D,s;Û)`、`E_δ(D,s;Ĺ)` 控制。
对固定参数与函数值，`δ>0` 时基本尺度随 `D→∞` 趋于零。

**还未完成对深度统一的离散误差递推。** 下一节已经证明跨拼接点的一侧导数、
可积性及带明确密度误差的离散 Buchstab 估计；仍需吸收该误差并处理随参数变化的
额外统一性因子。基本尺度的连续积分控制本身不保证深度统一的离散比较。
本节新增 19 项内核审计通过，筛法审计累计 460 项。

## 已补完：跨辅助函数拼接点的离散 Buchstab 比较

`AuxiliaryJunction.lean` 证明 `Û,Ĺ` 在整个分析区间内的一侧导数，包括各自
`3,2` 拼接点处的左导数。跳跃源项在拼接点取零；其与连续可测参数函数复合后，
仍在紧区间上可积。下支源项使用由初始公式构造的上支连续延拓，避免要求
原上支级数在参数 `1` 的取值满足未证明的连续性。

`AuxiliaryWeightDerivative.lean` 给出 `a_δ(t) H(t-1)` 的左导数。
与递减筛参数复合后，得到右导数 `auxiliaryPrimeSlope`，并证明其非负性及可积性。
`BuchstabAuxiliary.lean` 因而可以直接使用现有 Abel–Buchstab 定理，
跨过辅助函数拼接点，对实际奇素数集合及实际 Buchstab 系数求和。

`AuxiliaryChildSum.lean` 把结果精确改写成子水平尺度之和。
记 `s=log D/log z`、`E(D,s;H)=(log D)^(-δ)sH(s)`，对于 `0≤δ≤1`，已证明

```text
Σ_(floor(w)<p≤z) E(D/p, log(D/p)/log p; Ĺ) c_p
  ≤ E(D,s; Û) + 2K E(D/z,s-1; Ĺ)/log w,      s≥3.
```

交换上下支的版本要求 `s>2`。常数 `K>0` 对水平、分割点、参数和有限奇素数集合统一，
前提还包括 `D>1`、`2≤w≤z`。

这里的 `1/log w` 终端误差没有被删去或假定可吸收。下一步仍需控制它和原连续
深度项产生的密度误差，并建立随深度统一的完整归纳，才能与增长深度尾界合并。
本节新增 22 项内核审计通过，筛法审计累计 482 项。

## 已补完：累计深度误差的单步比较

`AuxiliaryMajorant.lean` 从辅助函数的实际定义证明
`U(s)≤sÛ(s)`（`s>1`）、`L(s)≤sĹ(s)`（`s≥2`）。
所有奇、偶有限连续部分和均不超过对应完整误差，因此同样受这些辅助函数控制，
常数不依赖部分和长度。`BuchstabPartials.lean` 对有限连续部分和直接求和，
得到不依赖截断项数的 Buchstab 常数；终端密度误差由上述统一辅助上界控制。

`RosserPartialDefect.lean` 定义实际累计量
`A_R(P,z,u,D)=Σ_(n<R) relativeStoppingMass(P,n,z,u,D)`，证明非负性、精确归一化、
累计递推和小素数前缀分割。当 `R>z` 时，它精确等于完整相对缺陷。
这里保留了深度零项，所以停止的下筛分支取值为 `1`；活动区间的部分和比较
不能无条件用于该停止分支。

`RosserPartialComparison.lean` 将实际累计递推、连续部分和主项及离散辅助误差
上界合并。给定子级的“连续部分和 + M·E”归纳假设，已证明父级上界由以下各项组成：

- 原小素数前缀；
- 对应父级连续部分和；
- `M·E` 父级辅助误差尺度；
- 连续主项的终端密度误差；
- 辅助误差自身的终端密度误差。

两种固定常数均不依赖实际深度截断或连续部分和长度。上筛单步要求 `s≥3`，
下筛单步要求 `s>2`，且均明确保留父分支活动条件和子级归纳假设。
**这仍是已证明的条件化单步，不是完成的统一归纳。** 后续还须控制前缀与停止分支，
加入定量筛证明中的额外因子 `(1+s^d/log D)^s`，并吸收两类终端误差。
不能仅将现有固定深度阈值用于增长深度，也不能据此删除 Richert–Bombieri 公理。
所需步骤包括辅助函数的定量移位比值、额外因子与加权辅助函数乘积的递减性、
积分收缩及大参数前缀控制；它们对应参考讲义的命题 13.1 与断言 14.5–14.6。
移位比值与端点前缀估计现已在后续章节证明，父参数转移和统一收缩仍待完成。
本节新增 20 项内核审计通过，筛法审计累计 502 项。

## 已补完：严格正性与固定参数下的积分差额

`ContinuousPositivity.lean` 从实际尾积分递推证明：正深度项
`f_(n+1)(s)` 在 `0<s<n+3` 内严格为正。选取足够大的奇数或偶数深度，
再使用已证明的可求和性，得到 `U(s)>0`（`s>1`）、`L(s)>0`（`s≥2`）。
因此上辅助函数在 `s>1`、下辅助函数在 `s>0` 上也严格为正。

`AuxiliaryStrictContraction.lean` 证明 `δ<1` 时子级权重
`(t/(t−1))^δ·(t−1)` 严格小于 `t`。在固定区间 `[s,s+1]`
积分其与 `t` 的正差，再用非负积分的区间单调性，得到一个 `η>0`：
对所有 `b≥s+1`，加权辅助积分至多是对应父级的
`s²·辅助函数(s) − b²·辅助函数(b) − η`。
上父级要求 `s≥3`，下父级要求 `s>2`。差额不随 `b` 增大而消失。

这里的 `η` 依赖固定的 `δ,s`；尚未给出随 `s` 增大的定量下界。
定量移位比值现已补完，但额外因子控制及统一收缩仍需完成，才能结束深度归纳。
这两份文件未引入额外数学假设，也没有替换现存两项未证明输入。
新增九项内核审计全部通过，筛法审计累计 511 项。

## 已补完：辅助函数的大参数定量下界

`AuxiliaryLowerStep.lean` 先从加权单调性推出两类辅助函数本身的单调性，
再在短区间 `[s,s+ε]` 上对延迟积分取下界，得到
`父级辅助函数(s) ≥ (ε/s)·子级辅助函数(s+ε−1)`。
该单步只要求 `ε≥0`；上父级要求 `s≥3`，下父级要求 `s>2`。

`AuxiliaryLowerBound.lean` 取
`c = min(上辅助函数(3), 下辅助函数(3)) > 0`，
对任意 `0<ε<1` 进行同时归纳。对于 `2<s≤S` 且
`s≤3+n(1−ε)`，两类函数都有同一个显式下界 `c·(ε/S)^n`。
特别地，`s≥3` 时可取 `n=ceil(s/(1−ε))`，得到
`c·(ε/s)^ceil(s/(1−ε))`。这个归纳没有隐含的深度相关阈值。

`AuxiliaryExponentialLowerBound.lean` 将自然数幂下界换为指数形式，
再取 `ε=1/log s`。当 `s≥3` 且 `log s≥2` 时，两类辅助函数均至少为
`c·exp(−s log s − s log log s − 6s)`。
这是由已构造函数直接证明的大参数下界，常数 `c` 的正性也已证明。

该下界可用于后续的大参数前缀比较；它没有自动完成辅助函数的定量移位比值，
也没有证明额外因子 `(1+s^d/log D)^s` 与加权辅助函数乘积的递减性、
收缩率或最终的离散误差吸收。
原有零点区域 `sorry` 与 Richert–Bombieri 公理仍未消除。
本节新增十二项内核审计全部通过，筛法审计累计 523 项。

## 已补完：额外因子的子级比较与完整误差下界

`AuxiliaryInflation.lean` 定义 `I(d,D,s)=(1+s^d/log D)^s`。
已证明它在 `D>1,s≥0` 时为正且至少为一；`d≥0` 时随 `s` 单调增长；
固定 `d,s` 而 `D→∞` 时趋于一。对 `d≥1` 及实际子筛参数
`t=log D/log p>1`，已证明
`I(d,D/p,t−1) ≤ (1+t^d/log D)^(t−1) ≤ I(d,D,t)`。

`InflatedAuxiliaryError.lean` 将该因子乘入原辅助误差尺度，
得到 `I(d,D,s)·(log D)^(-δ)·s·辅助函数(s)`。
已证明非负性、对子级误差的实际比较及固定参数下趋零（`δ>0`）。
子级比较保留指数 `t−1`，没有将它提前放宽为 `t`。

`AuxiliaryInflationDerivative.lean` 对
`I_a(s)=(1+(s+a)^d/log D)^s` 证明准确导数公式；
当 `d≥0,a≥0,s>0,D>1` 时，得到
`I_a'(s) ≤ (d+1)·I_a(s)·log(1+(s+a)^d/log D)`。
该结论包括后续使用的移位 `a=0,1`，但尚未结合辅助函数移位比值
证明 `I_a(s)·s²·辅助函数(s)` 递减。

`InflatedAuxiliaryLowerBound.lean` 将额外因子的指数下界与上一节结合。
设 `L=log D`，当 `D>1,s≥3,log s≥2` 时，两类完整辅助误差均至少为
`c·s·L^(-δ)·exp(s·((d−1)log s−log log s−log L−6))`。
这提供了后续大参数前缀比较的显式下界；尚需选择分割参数，
验证下面的比较指数足够小，并完成其余误差吸收。
本节新增十六项内核审计全部通过，筛法审计累计 539 项。

## 已补完：实际大参数缺陷的指数比较

`FactorialExponentialTail.lean` 从指数级数在 `N` 处的估计证明
`x^N/N! ≤ exp(N·(1+log x−log N))`（`x>0,N>0`）。
它进一步证明：若 `B>1,s≥6,s−3≤N≤s`，则
`B²·(log B)^N/N! ≤ exp(2log B+3log s+s·(2+log(1+log B)−log s))`。
取整误差已显式计入，保留了比较所需的 `−s log s` 主项。

`RosserParameterExponential.lean` 取实际深度 `N=floor(s)−2`，
证明其边界及严格筛水平条件，使初始停止深度段精确消失。
当 `s=log D/log(z+1)≥6` 时，完整实际相对缺陷满足上述指数上界，
其中 `B=depthMassMajorant K z`，常数 `K>0` 对全部奇素数集合和两种筛方向统一。

`RosserAuxiliaryComparison.lean` 将该上界与完整辅助误差下界结合。
设 `L=log D`，对 `s≥6,log s≥2`，实际缺陷不超过 `exp(R)` 倍的
上辅助误差，也不超过 `exp(R)` 倍的下辅助误差，其中
`R=2log B+2log s+δlog L−log c+s·(8+log(1+log B)+log log s+log L−d log s)`。
这个比较已经作用于实际离散缺陷，且没有子级归纳假设。

增长参数范围内 `R` 的控制现已在下一节证明。后续仍须将该估计应用于
递归小素数前缀的具体截断点，并处理辅助函数移位比值、乘积递减性和整个深度归纳。
原来的 `sorry` 与额外公理继续保留，最终定理未被标为已完成。
本节新增九项内核审计全部通过，筛法审计累计 548 项。

## 已补完：增长大参数区间的统一缺陷吸收

`ComparisonEnvelope.lean` 用 `comparisonEnvelope` 控制上一节的 `R/s`。
该函数在 `s≥e`、`d≥1` 时单调递减；因此在分割点处的上界
可同时控制所有更大的筛参数。`DepthMassLevelBound.lean` 证明
`B(z)≤C·log D`，其中 `C≥1` 只依赖固定的一维密度常数，消除了上界的截断点依赖。

`GrowingSieveParameter.lean` 定义 `σ(d,L)=L^(1/d)·log L`，`L=log D`，
证明其趋于无穷以及比较所需的一阶、二阶对数极限。
`GrowingEnvelopeLimit.lean` 证明分割点处的上界加上
`(d−2)log log L` 后趋于 `8+log(1/d)`。
因此 `d>2` 时，该上界最终不超过 `−1`。

`RosserGrowingParameter.lean` 将这些结论用于实际离散缺陷：
对每个固定 `d>2` 和实数 `δ`，存在统一的充分大水平阈值；
对所有 `z≥2`、`s=log D/log(z+1)≥σ(d,log D)`、奇素数集合及两种筛方向，
实际完整相对缺陷同时不超过
`exp(−s)·上完整辅助误差(d,δ,D,s)` 和
`exp(−s)·下完整辅助误差(d,δ,D,s)`。
这个估计不依赖子级归纳假设，也没有使用固定深度的非统一阈值。

**增长大参数区间的缺陷吸收已经证明。** 递归中的小素数截断及其取整后前缀估计
现已在下一节证明；后续仍须将其转移到父参数，并与中间素数区间的比较合并。
定量移位比值现已在后续章节证明；乘积递减性、统一严格收缩和全部深度归纳仍未完成；
零点区域 `sorry` 与 Richert–Bombieri 公理仍然保留。
本节新增十九项内核审计全部通过，筛法审计累计 567 项。

## 已补完：取整后的小素数前缀估计

`RosserPrefixNormalization.lean` 证明任意累计深度缺陷不超过完整缺陷，
并将活动小截断处的递归前缀精确写为
`V(m+1)/V(z+1) · 累计缺陷(N+1,m+1)`。
这保留了正确的归一化因子，并明确排除了小截断处的下筛停止分支。

`GrowingPrefixCutoff.lean` 取 `σ=σ(d,log D)`、
`w=exp(log D/(2σ))`、`m=floor(w)`。
已证明 `d>1` 时 `σ(d,L)/L→0`、`w→∞`，且充分大水平下
`m≥2`、`σ≤t=log D/log(m+1)≤2σ`、`(m+1)²<D`。
因而取整后的小截断确实满足增长大参数估计和活动分支条件。

`GrowingPrefixRatio.lean` 用已经证明的一维密度条件控制筛乘积比：
当 `m≤z` 时，它不超过 `4(1+K/log 2)·σ/s`，
其中 `s=log D/log(z+1)`，常数 `K>0` 对全部素数集合统一。

`RosserGrowingPrefix.lean` 由上述精确关系及增长大参数缺陷估计推出：
对固定 `d>2,δ`，在一个统一的充分大水平阈值以后，对任意累计深度、
奇素数集合、`m≤z` 和两种筛方向，实际递归前缀同时满足
`前缀 ≤ C·(σ/s)·exp(−σ)·上完整辅助误差(D,t)` 及对应的下辅助误差界，
其中 `t∈[σ,2σ]`，`C=4(1+K/log 2)`。

**前缀仍以端点 `t` 的辅助误差计量。** 将其吸收到父参数 `s` 的误差中，
还需要证明额外因子与加权辅助函数乘积的递减性。
定量移位比值现已补完；中间素数区间的误差吸收及完整深度归纳仍未完成。
本节新增十五项内核审计全部通过，筛法审计累计 582 项。

## 已补完：辅助方程的守恒式与指数衰减

`AuxiliarySumPairing.lean` 定义实际辅助函数之和
`Q(s)=上辅助误差(s)+下辅助误差(s)`，证明它在 `s>1` 连续、严格正且递减，
并在 `s>3` 满足 `s Q'(s)=−2Q(s)−Q(s−1)`。
取伴随多项式 `q(s)=s²−2s+1/2`，从原连续误差已经证明的守恒式，
经分部积分得到精确恒等式
`s q(s) Q(s)=∫[s−1,s] q(t+1)Q(t) dt`。
这里的零常数来自原守恒式，不是另行假设的无穷远边界条件。

`AuxiliarySumDecay.lean` 由此得到 `s Q(s)≤3Q(s−1)`（`s>3`），
以及 `s Q(s)≤(1+4/s)Q(s−1)`（`s≥4`）。
延迟方程随后给出 `exp(s/3)s²Q(s)` 在 `[4,∞)` 递减，
故 `Q(t)≤exp(−(t−s)/3)Q(s)`，其中 `4≤s≤t`。

`AuxiliaryExponentialDecay.lean` 把该单位区间衰减代回守恒式，
将积分上界中的系数改进为 `7/8`，得到 `s Q(s)≤Q(s−1)`（`s≥16`）。
因此 `exp(s)s²Q(s)` 在 `[16,∞)` 递减，且两支实际辅助函数均满足
`辅助误差(s)≤C exp(−s)`（`s≥16`），其中
`C=exp(16)Q(16)>0`。
这补齐了辅助延迟方程的指数衰减条件。

参考证明是 Suzuki 的 [Rosser–Iwaniec 筛讲义](https://www.suzuyu1729.com/notes/NagoyaSeminar2022/RosserIwaniec.pdf)
第 10.11、13 节；这里的初步界与指数衰减直接由本项目已构造的函数证明。
更强的 `Q(s−1)/Q(s)≈s log s` 估计已在后续章节分上下界补完。
上下辅助函数的可比性及交叉移位比值归约已在下一节补完。
因此尚不能宣称额外因子乘积递减、前缀向父参数的转移或累计深度归纳已经完成。
本节新增二十六项内核审计全部通过，筛法审计累计 608 项。

## 已补完：两支辅助函数的统一可比性

`AuxiliaryDifferencePairing.lean` 定义两支实际辅助函数之差 `P`，
从原连续误差之差的导数证明另一条精确守恒式
`∫[s−1,s] P(t) dt=−sP(s)`（`s>3`）。
另一方面，伴随多项式在单位区间内严格递增，因此和函数满足
`∫[s−1,s] Q(t) dt<sQ(s)`。

`AuxiliaryComparability.lean` 证明相对差值 `|P(s)|/Q(s)` 的最大值原理：
在初始区间 `[2,3]` 上的任何非负上界都控制整个 `[2,∞)`。
证明在任意有限区间上取实际最大值，若它超过初始上界，
差函数的守恒式与和函数的严格积分不等式就产生矛盾。
初始区间的紧致性和两支函数的严格正性于是给出统一的 `η<1`，
以及 `c=(1−η)/2>0`，使任意 `s≥2` 都有
`cQ(s)≤上辅助误差(s)≤Q(s)` 和 `cQ(s)≤下辅助误差(s)≤Q(s)`。
这也证明了上下辅助函数之间的双向常数倍比较。

`AuxiliaryShiftComparison.lean` 令 `r(s)=Q(s−1)/(sQ(s))`，
证明任意 `s≥3` 时，两种交叉移位比值
`下辅助误差(s−1)/(s·上辅助误差(s))` 及其上下交换形式
都介于 `c r(s)` 与 `r(s)/c` 之间。
已知的 `r(s)≥1/3` 进一步给出统一常数 `k>0`，使 `s>3` 时
`k s·上辅助误差(s)≤下辅助误差(s−1)`，且上下交换后同样成立。
以上所有常数都独立于参数和筛水平，没有引入比较性假设。

标量比值的双向估计 `r(s)≈log s` 当时尚未完成；
下一节已补出对数下界并将其传递到交叉移位比值。
对数上界亦已在后续章节证明；一直到增长截断 `σ(log D)` 的额外因子乘积递减性仍待证明。
本节新增十七项内核审计全部通过，筛法审计累计 625 项。

## 已补完：标量及交叉移位比值的对数下界

`AuxiliaryBarrierPhase.lean` 构造
`φ(a,C,s)=a s log s+C s` 和 `W(a,C,s)=Q(s) exp(φ(a,C,s))`。
已证明其导数公式及驻点条件
`W'(s)=0 ⇔ r(s)=φ'(s)−2/s`。
`AuxiliaryBarrierKernel.lean` 证明对 `a≥0,s≥4,0≤u≤1` 的显式单位区间界
`q(s+u) exp(φ(s−1)−φ(s−1+u))≤q(s) exp(−v u)`，
其中 `v=φ'(s)−(2a+4)/s`，且常数不依赖线性系数 `C`。

`AuxiliaryBarrierIntegral.lean` 将其积分为
`I≤q(s)(1−exp(−v))/v`。
若 `W` 在前一个单位区间递减，则标量守恒式给出对应的积分比较。
在驻点处，这强制
`1≤(φ'(s)−2/s)(1−exp(−v))/v`。

`AuxiliaryBarrierCritical.lean` 对 `a=1/2`、非正的线性系数和 `log s≥20`
证明该因子严格小于一，从而排除上述驻点。
`ContinuousFirstZero.lean` 严格构造连续函数从负值变到非负值时的第一个零点，
保证在其之前始终为负。

`AuxiliaryLogarithmicLower.lean` 取 `A=exp(20)` 和
`C=(log(A+1)+1)/2+1>0`，证明下比较函数在 `[A,A+1]` 的导数严格为负。
若之后导数变为非负，首次零点之前的递减性就与驻点排除定理矛盾。
由此得到整个 `s≥A` 上的
`r(s)>(1/2)log s−C`，以及充分大参数下的
`r(s)≥(1/4)log s`。
结合两支辅助函数的统一可比性，现已证明存在统一 `k>0`，使充分大参数下
两种交叉移位比值均不小于 `k log s`。

**对数下界已经完成，不再作为待证输入。** 对数上界在下一节补完；
增长区间内额外因子的对数导数控制、加权乘积递减性、前缀转移、
严格收缩的统一误差及累计深度归纳也仍需完成。
本节新增二十六项内核审计全部通过，筛法审计累计 651 项。

## 已补完：对数上界及半轴上的统一移位估计

`AuxiliaryBarrierKernelLower.lean` 对 `0≤u≤1/2,s≥4` 证明
`q(s) exp(u/s)≤q(s+u)`，以及相位增量的对应上界。
由此得到前半个单位区间上的核下界，指数速率为
`v=φ'(s)−(1+a/2)/s`。
`AuxiliaryBarrierIntegralLower.lean` 将它积分为
`I≥q(s)(1−exp(−v/2))/v`，并证明当比较函数此前递增时，
驻点条件强制 `r(s)(1−exp(−v/2))/v≤1`。

`AuxiliaryBarrierUpperCritical.lean` 取 `a=8` 和非负线性系数，
证明该因子对 `s≥4` 严格大于一，从而排除上述驻点。
`AuxiliaryLogarithmicUpper.lean` 在初始紧区间 `[4,5]` 上取标量比值的实际最大值，
据此选取正的线性系数，使上比较函数的导数初始严格为正。
首次零点论证将它延伸到整个 `[4,∞)`，得到
`r(s)<8 log s+C`，并推出充分大参数下 `r(s)≤9 log s`。
结合已证明的下界，标量比值现在具有完整的双向对数估计。

`AuxiliaryUniformShift.lean` 进一步对初始紧区间内的 `r(s)/log s`
取实际最大值、最小值，证明同一组正数 `k,K` 对所有 `s≥3` 都成立：
`k log s≤r(s)≤K log s`。
两支辅助函数的统一可比性随后给出对所有 `s≥3` 的两种交叉移位估计：
子项介于对应父项的 `k s log s` 与 `K s log s` 倍之间。
常数不依赖参数、筛水平或素数集合。

**`s≥3` 上的统一双向移位估计已经完成。**
增长区间内额外因子的对数导数控制、加权乘积递减性与前缀向父参数的转移
由下一节补完。累计深度归纳仍未完成。下支 `2<s<3` 的衔接
由后续初始区间模块证明；本节没有将它隐含在 `s≥3` 的结论中。
本节新增二十项内核审计全部通过，筛法审计累计 671 项。

## 已补完：增长区间内的递减性与前缀父参数估计

`InflationLogBounds.lean` 对 `L=log D` 按
`s≤L^(1/(2d))` 与相反情形分段，构造显式预算 `b_d(L)`，证明
`log(1+(s+a)^d/L)≤b_d(L) log s` 对 `3≤s≤2σ(d,L)`、`0≤a≤1`
一致成立。`InflationSlopeLimit.lean` 证明 `b_d(L)→0`，并由此得到
额外因子的对数导数对任意给定 `ε>0` 最终都不超过 `ε log s`。
筛水平阈值不依赖区间内的参数或移位。

`InflatedAuxiliaryMonotonicity.lean` 将它与已证明的交叉移位下界结合，
直接证明对每个 `d>0`、充分大的 `D`，两支函数
`(1+(s+a)^d/log D)^s · s²H(s)` 在 `[3,2σ(d,log D)]` 上递减，
且结论对所有 `a∈[0,1]` 同时成立。
`InflatedAuxiliaryTransport.lean` 取零移位，推出
`E(D,t)≤(s/t)E(D,s)`，其中 `3≤s≤t≤2σ`，`E` 是完整的辅助误差。

`RosserParentPrefix.lean` 把上述结论用于实际取整截断
`m=floor(exp(log D/(2σ)))`。在 `m≤z` 且父参数 `s≥3` 时，
先前的 `C(σ/s)exp(-σ)E(D,t)` 前缀界变为
`C exp(-σ)E(D,s)`，因为 `σ≤t`。
这同时适用于两支辅助函数、任意筛符号、任意累计深度和任意奇素数集合，
常数 `C` 不依赖这些变量。进一步证明对每个固定 `ε>0`，
充分大水平下前缀不超过 `εE(D,s)`，阈值仍与累计深度和素数集合无关。

这里完成了真实递归前缀的父参数估计；中间素数区间的定量严格收缩、
密度误差吸收和完整累计深度归纳仍需证明。下支 `2<s<3` 的衔接见下一节。
本节没有消除 Richert–Bombieri 公理或零点自由区域的 `sorry`。
新增十二项定理均通过内核公理审计，筛法审计累计 683 项。

## 已补完：初始区间衔接与带额外因子的子筛权重形状

`AuxiliaryLowerInitialShift.lean` 用初始公式与已有递减性，对 `2<s≤3`
证明 `sĹ(s)/4≤Û(s−1)≤A sĹ(s)/(2Ĺ(3))`。
与 `s≥3` 上的估计拼接后，下支的双向对数交叉移位界现在对所有 `s>2`
成立。`InflationInitialSlope.lean` 在初始区间直接使用
`slope≤(d+1)4^d/log D`，将一致的 `ε log s` 控制延伸到 `s=2`。

`InflatedLowerMonotonicity.lean` 只在区间内部使用导数，端点使用连续性，
从而证明下支的带移位加权乘积在整个 `[2,2σ]` 递减。
它也将下支的完整误差转移 `E(D,t)≤(s/t)E(D,s)` 延伸到 `s≥2`。
这里没有假设延迟微分方程在端点二成立。

定义子筛误差的父水平控制权重
`q_H(t)=childInflation(d,D,t) · childAuxiliaryWeight(δ,t) · H(t−1)`。
`InflatedChildWeight.lean` 证明精确分解
`t q_H(t)=Λ₁,H(t−1) · (t/(t−1))^(δ+1)`，其中 `Λ₁,H` 是移位一的加权乘积。
两个非负递减因子的乘积给出下支子权重在 `[3,2σ+1]` 上的加权递减性，
以及上支子权重在 `[4,2σ+1]` 上的同样结论。

`InflatedChildInitialWeight.lean` 在 `2<t≤4` 使用 `Û(t−1)=A/(t−1)²`，
将上支权重化为显式函数。该函数的对数导数为
`slope(d,D,1,t−1)−(δ+1)/(t(t−1))`，对固定 `δ>−1` 与充分大水平非正。
在四处拼接后，上支子权重的加权递减性覆盖整个 `(2,2σ+1]`。
下支结论允许 `δ≥−1`；两者都覆盖后续所需的 `0<δ<1`。

`InflatedPrimeWeight.lean` 将加权递减性和非负性转回素数大小变量，
证明两种实际控制权重都单调递增，并满足加权 Buchstab 分部求和所需的
`f(t)log z≤f(z)log t`。水平阈值对允许的素数区间统一。

新增二十一项定理通过内核公理审计，累计 704 项。
**两种子筛权重的形状条件已完成，带额外因子的积分估计尚未完成。**
导数与积分的连接由下一节完成。仍须处理定量严格收缩、密度误差吸收及深度归纳，
才能用真实证明替代 Richert–Bombieri 公理。

## 已补完：带额外因子的 Buchstab 子筛求和界

`BuchstabMonotoneRight.lean` 证明单调权重的内部右导数非负。
对可积导数取正部，不改变内部的导数关系，并允许两个端点处的值任意，
从而得到仅要求权重本身单调的带权分部求和定理。

`ChildInflationDerivative.lean` 证明子额外因子的导数、对数导数的连续性，
以及合成筛参数后的导数及连续性。
`InflatedWeightRegularity.lean` 将它与已有辅助权重的单侧导数相乘，
证明实际子筛控制权重连续，具有显式可积右导数；辅助函数拼接点仍被包括在内。

`BuchstabInflatedParameter.lean` 据此给出参数形式的分部求和界。
`BuchstabInflated.lean` 对两支实际辅助函数消去全部正则性、源项可积性、
单调性与对数增长假设。对固定 `d>0` 和允许的 `δ`，存在统一正数 `K`，
充分大的筛水平下，令 `s=log D/log z`、`b=log D/log w`，有
`Σ_p q_H(log D/log p) coefficient(p) ≤ (1/s)∫_s^b q_H(t)dt + 2K q_H(s)/log w`。
下支子权重要求 `s≥3,δ≥−1`，上支子权重要求 `s>2,δ>−1`；
两者允许整个 `b≤2σ+1` 的增长区间，且常数不依赖素数集合、分割点或筛水平。

`InflatedAuxiliaryChildSum.lean` 进一步对真实子水平 `D/p` 的完整误差逐项比较，
得到同一积分与端点损失之和乘以 `(log D)^(-δ)` 的实际误差和上界。
这里要求 `d≥1`，正是已证明的真实子水平额外因子比较所需条件。
本节的专门化结论不再要求用户提供导数、可积性或形状假设。

新增十六项定理全部通过内核公理审计，累计 720 项。
**带额外因子的离散求和到积分的步骤已完成；积分的定量严格收缩由下一节补完。**
还须将端点密度损失吸收到父参数的完整误差中，再与前缀界、
停止分支和累计深度归纳合并；`2 exp(γ)` 的常数校准仍未完成。

## 已补完：定量严格收缩与真实子筛误差和

上一节记录的待验证路线现已形式化。
`InflationContractionGap.lean` 令 `x=t^d/log D`、`α=1−δ`，用实指数
Bernoulli 不等式证明 `childAuxiliaryWeight(δ,t)≤t−α`，并构造
`g=(x+α/t)/(1+x)`。它同时保留子额外因子分母和对数误差指数的损失，
满足 `α/t≤g≤1`。

`InflationSlopeGap.lean` 对每个固定 `d>0`、`0<α≤1`、`k>0`，证明
充分大水平下 `slope(d,D,0,t)≤(k log t)g/2` 对 `2≤t≤2σ` 一致成立。
证明分为固定初始区间、其后的 `x≤1` 和 `x≥1` 三种情况，分别使用
紧区间上的 `O(1/log D)` 控制、`log(1+x)≤x` 与已有一致小斜率界。

`InflatedAuxiliaryDissipation.lean` 将它与真实交叉移位下界结合，证明
子权重 `q_J(t)` 不超过父加权乘积负导数的 `1−α/(4B)` 倍，`t≤B`。
`InflatedAuxiliaryIntegral.lean` 证明该导数的精确积分恒等式；只在区间内部
使用延迟微分方程，因而不要求上支端点三可微。
`InflatedAuxiliaryContraction.lean` 得到两支的统一定量严格收缩：
`∫_s^b q_J≤(1−α/(4B))(I(s)s²H(s)−I(b)b²H(b))`，其中 `s≤b≤B≤2σ`。
上筛父参数允许 `s≥3`，下筛允许 `s>2`，且显式保留了非负终端尾项。

`InflatedContractedChildSum.lean` 把这一结论接回真实子水平 `D/p` 的误差和：
`Σ_p E(D/p,child) coefficient(p)≤(1−(1−δ)/(4B))E(D,s)+terminalLoss`。
这里 `d≥1,0≤δ<1`；常数和水平阈值对允许的分割点及有限奇素数集合统一。
终端损失仍为 `(log D)^(-δ) 2K q_J(s)/log w`，没有将它省略或当作已吸收。

**积分的定量严格收缩已经完成。** 终端密度损失、连续主项的密度误差
与前缀的吸收由下一节完成；停止分支、统一累计深度归纳与常数校准仍未完成。
新增十九项定理全部通过内核公理审计，累计 739 项。

## 已补完：全部单步误差吸收与实际父截止点对齐

继续新增九个模块、二十四项定理。设 `L=log D`、`σ=L^(1/d) log L`、
`w=exp(L/(2σ))`、`m=floor w`，`E` 是已有带额外因子的完整辅助误差。

`GrowingDensityBudget.lean` 证明 `(2σ)^3 log(2σ)/L^β→0`，条件为 `3/d<β`。
`InflatedTerminalLoss.lean` 与 `InflatedTerminalAbsorption.lean` 利用一致交叉移位上界，
把真实子筛误差和的终端密度损失压到任意给定的 `ε E/(2σ)` 内。
`InflatedAbsorbedChildSum.lean` 因而得到上下两支的实际子水平误差和上界
`Σ_p E(D/p,child) coefficient(p)≤(1−(1−δ)/(16σ))E(D,s)`，
其中 `d>3,0≤δ<1`。这两式已不再带终端损失项。

`ContinuousTerminalLoss.lean` 与 `ContinuousTerminalAbsorption.lean` 处理连续主项
有限和的另一类密度误差；所需条件是 `3/d<1−δ`。阈值对允许的父参数和连续
截断深度一致。`RosserInflatedStep.lean` 将两类吸收接回真实累计缺陷的活动递推，
保留实际小素数前缀，余下系数为 `1−(1−δ)/(32σ)`。

`RosserPrefixAbsorption.lean` 将下支前缀的父参数估计延伸至 `s≥2`，并利用
`σ exp(−σ)→0`，把两支前缀压到任意给定的 `ε E/σ` 内，阈值不依赖累计深度。
`RosserInflatedParentStep.lean` 进一步使用连续有限和与完整误差的单调性，
把中间估计的 `s(D,z)` 转移到实际父截止点参数 `s(D,z+1)`，合并前缀后仍保留
`1−(1−δ)/(64σ)` 的严格收缩。

最终两式的条件为 `d>3,0≤δ<1,3/d<1−δ,M≥1,w≤z`，
上支父参数 `s(D,z+1)≥3`，下支 `s(D,z+1)>2`；实际子缺陷的归纳假设显式保留。
结论分别是父缺陷不超过 `U_N(s)+M(1−(1−δ)/(64σ))E_upper(D,s)` 和
`L_(N+1)(s)+M(1−(1−δ)/(64σ))E_lower(D,s)`。

**这完成了活动分支的一次递推，没有完成全部深度归纳。** 仍须处理停止分支与
有界子水平、连续有限和在停止点的缺额、初始区间的统一组装，随后完成总缺陷估计、
`2e^γ` 常数校准及引理 9 的最终组合。本节二十四项定理全部通过标准公理审计，累计 763 项。

## 尚未完成：零点自由区域

位置：`ChenTheorem/Lemma6/ZeroFreeRegion.lean`，
`primitive_zero_free_region : PrimitiveZeroFreeRegion`。

这里需要证明一个包含三个部分的经典分析包：

1. 本原 Dirichlet L 函数的高度相关非例外零点自由区域，宽度为
   `cHeight / log (q * (abs t + 2))`。
2. 对每个固定正整数 `N`，处理可能例外实零点的 Siegel 界，宽度为
   `cSiegel * q ^ (-1/N)`；常数允许非有效。
3. 在上述两种宽度的最小值的一半区域内，证明所需 `L'/L` 上界。

现有 mathlib 的 `re s ≥ 1` 非消失定理不能直接给出这些随导数与高度变化的定量结论。
本次迁入的 Riemann zeta 零点估计也不能直接用于任意 Dirichlet 特征。
后续应从非例外区域、例外零点的 Siegel 界及对数导数估计分别建立证明，
再组装现有 `PrimitiveZeroFreeRegionDataAt N` 接口。

## 尚未完成：Richert 加权筛

位置：`ChenTheorem/Lemma9/RichertBombieri.lean`，
`axiom richert_bombieri_equation26`。

它断言原变量与固定平移两类问题的联合筛下界，承载了论文引理 9 的核心内容。
两个 `eventually_...` 声明只是该公理的专门化包装。

对照 `../latex/part2_en.tex` 引理 9 与 `../latex/part3_en.tex` 开头，剩余工作包括：

1. 将已经构造的 `rosserEval` 主项与线性筛函数 `F`、`f` 的尖锐渐近界联系起来，
   包括所需的统一误差与筛区间。有限上下筛权、截断支集、精确主项恒等式、
   实端点定量一维密度条件、非负误差递推及可微权重的带权分部求和已完成；
   筛参数换元、条件化的单步连续比较和固定截断前缀误差消失也已完成；
   实际连续递推项、形状性质和奇偶有限和的延迟微分关系也已证明；
   连续级数的收敛、一致收敛、极限连续性及延迟微分方程也已证明；
   无穷远衰减、下筛端点值与初始区间公式也已证明；
   上筛误差在 `s=3` 的导数拼接及实际误差函数的单步比较已证明；
   上支小参数区间已一致归约到严格立方根截断处的 `s>3` 估计；
   停止深度的精确展开、阶乘尾项及大参数粗界也已证明；
   深度奇偶结构、所有固定正深度及固定有限深度和的统一比较已证明；
   任意固定上筛深度从大于三到整个大于一区间的比较转移已证明；
   递归连续深度项跨拼接点的 Buchstab 比较已证明；
   增长深度以后的统一消失尾界已证明；前段增长项数的定量误差、
   总缺陷尖锐估计与 `2e^γ` 常数校准仍未完成。
2. 原变量和固定平移两种对数加权序列的乘法密度、精确余项、普通计数
   转换及方程 (25) 所需的奇异级数归一化均已完成。这些输入仍需与
   第一项中尚未证明的尖锐主项界连接。
3. 两种情形的 BV 余项、`T = x/log⁴ x` 截断及非既约类损失已经统一
   吸收到相应奇异级数尺度上的任意小误差，并为 `Q = floor(x^a)`、
   `1/3 < a < 1/2` 验证了主筛与全部中间素数的子筛水平。仍需将
   尖锐主项界与筛乘积归一化合并，令参数趋近论文所用端点。
4. 按论文在 `s = 5` 的主筛下界和固定中间素数的上界进行组合，
   完成误差、变量替换和统一的任意 `δ > 0` 扣损，得到现有接口的原陈述。

仅证明 BV 及其上述有限加权筛推论仍不会自动消除这一公理；
尖锐主项与完整渐近参数组装仍必须证明。

原始参考文献为 Richert, *Selberg's sieve with weights*, Mathematika 16 (1969),
1–22（[期刊页面](https://www.cambridge.org/core/journals/mathematika/article/abs/selbergs-sieve-with-weights/045BB37A164D6FF9F44469FD8742D824)）。

## 可重复验证

在 `formal/` 中执行：

```text
python scripts/check_dependencies.py
lake build
lake env lean AuditAnalysis.lean
lake env lean AuditSieve.lean
lake env lean Audit.lean
```

`AuditAnalysis.lean` 验证 PNT、Mertens 第二及第三定理、Perron 公式及自然数
截断 Euler 素数乘积归一化的实际内核公理闭包，允许且仅允许
`propext`、`Classical.choice`、`Quot.sound`。这八项已经通过审计。

`Audit.lean` 是整个项目的完成门槛：主定理或定量形式只要仍含额外公理就会报错。
最新完整构建通过（4024 个构建任务），依赖检查通过（292 个源码文件），
`AuditAnalysis.lean` 的 8 项与 `AuditSieve.lean` 的 763 项均通过。最新 `Audit.lean` 的六个
有限筛检查通过，五个最终定理检查仍明确失败（退出码 1），不能忽略。
这是当前最终定理的实际内核结果：

| 定理 | 标准公理以外的依赖 |
| --- | --- |
| `Chen.chen_theorem` | `sorryAx`、`Chen.richert_bombieri_equation26` |
| `Chen.chen_twin` | `sorryAx`、`Chen.richert_bombieri_equation26` |
| `Chen.chenCount_lower` | `sorryAx`、`Chen.richert_bombieri_equation26` |
| `Chen.chenCountShift_lower` | `sorryAx`、`Chen.richert_bombieri_equation26` |
| `Chen.BombieriVinogradov.bombieriVinogradov` | `sorryAx` |

只有这些额外依赖全部消失且完整构建通过，才满足本项目的最终完成目标。
